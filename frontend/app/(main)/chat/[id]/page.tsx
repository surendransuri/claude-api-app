// Unused - chat route is handled by app/chat/[id]/page.tsx
// Delete this file before running `next build`
import { redirect } from 'next/navigation'
export default function UnusedChatPage() {
  redirect('/')
}
